-- Схема таблиц парсера больше НЕ живёт здесь.
--
-- Раньше этот файл дублировал те же CREATE TABLE, что и alembic-миграция
-- бэкенда, и это были две расходящиеся копии одной правды: применялась
-- миграция, а читали часто отсюда.
--
-- Актуальный источник:
--   backend/app/db/migrations/versions/c1a4f9d3e7b2_add_telegram_parser_tables.py
--
-- Таблицы создаются автоматически при старте бэкенда (alembic upgrade head
-- в backend/entrypoint.sh), отдельно ничего накатывать не нужно.
